# Ejemplo de variables para despliegue en Docker
# Copia este archivo a .env y ajusta valores

# App
NODE_ENV=production
HOST=0.0.0.0
PORT=3000
PORT_FALLBACK=3001

# Dominio público para HTTPS (Let’s Encrypt)
DOMAIN=api.tu-dominio.com
CERTBOT_EMAIL=tu-correo@example.com

# MongoDB (usa el servicio "mongo" del docker-compose o una URI externa)
MONGODB_URI=mongodb://mongo:27017
MONGODB_DB=sapo

# Sesiones (cambiar el secreto!)
SESSION_SECRET=pon_un_secreto_muy_largo_y_aleatorio
# Si Nginx termina TLS (https), usa true y SameSite=None. Si solo usas http, pon false y SameSite=Lax
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_SAMESITE=none

# Ajustes de sincronización/externos (si aplican)
# EXTERNAL_EMP_API_URL=https://example.com/api
# EXTERNAL_EMP_API_TIMEOUT_MS=30000
# ATT_SYNC_ENABLED=true
# ATT_SYNC_INTERVAL_MINUTES=120
# ATT_SYNC_RUN_AT_START=true
